<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lliira.bookworm.core.persist.mapper.UserMapper">

	<resultMap type="User" id="UserMap">
		<result property="id" column="user_id" />
		<result property="name" column="user_name" />
		<result property="email" column="email" />
		<result property="password" column="password" />
		<result property="registerTime" column="register_time" />
	</resultMap>

	<select id="selectUserById" parameterType="int" resultMap="UserMap">
    <![CDATA[
        SELECT * FROM ${core-schema}users WHERE user_id = #{id}
    ]]>
	</select>
	
	<select id="selectUserByEmail" parameterType="string" resultMap="UserMap">
    <![CDATA[
        SELECT * FROM ${core-schema}users WHERE email = #{email}
    ]]>
	</select>

	<insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="id" keyColumn="user_id">
    <![CDATA[
        INSERT INTO ${core-schema}users (user_name, email, password, register_time)
        VALUES (#{name}, #{email}, #{password}, #{registerTime})
    ]]>
	</insert>

	<update id="updateUser" parameterType="User">
    <![CDATA[
        UPDATE ${core-schema}users
        SET user_name = #{name}, email = #{email}, password = #{password} 
        WHERE user_id = #{id}
    ]]>
	</update>

	<delete id="deleteUser" parameterType="int">
    <![CDATA[
        DELETE FROM ${core-schema}users WHERE user_id = #{id}
    ]]>
	</delete>

</mapper>